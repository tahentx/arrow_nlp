from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Topic modeling the Apache Arrow repo (commit comments and pull requests) using non-negative matrix factorization.',
    author='Todd Hendricks',
    license='',
)
